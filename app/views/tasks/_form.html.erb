<%= form_for(@task) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>

 
    <%= f.label :name %>
    <%= f.text_field :name %>
 
  
  <div class="actions">
    <%= f.submit "Create Task", class: "btn btn-primary" %>
  </div>
<% end %>
